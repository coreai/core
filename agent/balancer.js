function balance (loads, nodes, timeout) {
    // balance loads when they are greater than average
    if (loads.average < loads.current) {
        // balancing the load occurs

        // use a timeout to rule out nodes that are far

        // get the nodes that are most capable

        // get the nodes with the lowest influence

        // determine candidates, choose one with p2c
        
        
    }

}