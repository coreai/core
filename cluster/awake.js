// get rules

// start network
    
// get cluster size

// determine identity


function Capablility (node) {
// Each node performs every role...but they are scored for how well they do it.
// they are given a capability score... 
// nodes do not score their own capability, the capability scoring is part of the proof of identity algorithm... 
// every time a node performs an action they identify(verify) another node and give it a capability score..
    const capability = null
}

const identity = { // a hash derived of the following...
    capability: location + specs,
    impact: null, // how many useful/liked actions?
    influence: null, // how many total actions?
}
    
// determine need

// assign role