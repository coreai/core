// balance... 
// used for load balancing server to server communication
// used for client-server selection
// Determines the flow of data, who gets what and when.
// Weigh the needs of the network against the influence of a node who is about to process some data

// weigh the needs of the network against the influence of the node
function balance (nodes) {

   // we use the power of 2 choices to balance the network

    if (influence < need && influence > cluster) {
        // only select nodes that have less influence than need and influence is not greater than the cluster size
    }
}

