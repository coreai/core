// are are the ratios correct?
function Need (ratio, nodes) {
    if (ratio != true) {
        // if the ratio is not met then meet it...

        // do what needs to be done to fulfill the ratios

    } else {
        
    }
}