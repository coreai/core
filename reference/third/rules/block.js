'use strict';

// blocks are objects
const block = {
    index: 0, // is a number
    time: time, // is a string of only numbers
    data: data, // is anything
    hash: index + time + data // is a unique identifier
  }

